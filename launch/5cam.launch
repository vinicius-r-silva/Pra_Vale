<launch>

	<!-- Reading the camera (trying) -->
	<node name="Camera" pkg="pra_vale" type="5cam.py" output="screen"/>

	<rosparam command="load" file="$(find pra_vale)/config/simulation_parameters.yaml" />

</launch>
