<launch>

	<!-- Reading the camera (trying) -->
	<node name="Camera" pkg="rosi_defy" type="5cam.py" output="screen"/>

	<rosparam command="load" file="$(find rosi_defy)/config/simulation_parameters.yaml" />

</launch>
